import { QuestionPrefixPipe } from './question-prefix.pipe';

describe('QuestionPrefixPipe', () => {
  it('create an instance', () => {
    const pipe = new QuestionPrefixPipe();
    expect(pipe).toBeTruthy();
  });
});
