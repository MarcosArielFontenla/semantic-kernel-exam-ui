import { ValidateAnswerDirective } from './validate-answer.directive';

describe('ValidateAnswerDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateAnswerDirective();
    expect(directive).toBeTruthy();
  });
});
