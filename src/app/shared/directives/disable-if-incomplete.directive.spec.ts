import { DisableIfIncompleteDirective } from './disable-if-incomplete.directive';

describe('DisableIfIncompleteDirective', () => {
  it('should create an instance', () => {
    const directive = new DisableIfIncompleteDirective();
    expect(directive).toBeTruthy();
  });
});
